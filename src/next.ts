/**
 * Next.js reference OAuth routes for the GitHub Discussions backend. Optional — use these if you don't
 * already have GitHub auth on your site. Import from `fuma-comment-github-discussions/next`.
 */

export { createOAuthRoutes } from "./oauth/routes";
export type { GitHubOAuthConfig, OAuthRoutes } from "./oauth/routes";
